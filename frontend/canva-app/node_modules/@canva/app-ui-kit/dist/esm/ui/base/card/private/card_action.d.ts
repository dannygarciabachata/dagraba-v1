import * as React from 'react';
type CardActionProps = {
    children: React.ReactNode
    position?: 'unset' | 'relative'
};
export declare const CardAction: ({ children, position, }: CardActionProps) => React.JSX.Element | null;
export {};
