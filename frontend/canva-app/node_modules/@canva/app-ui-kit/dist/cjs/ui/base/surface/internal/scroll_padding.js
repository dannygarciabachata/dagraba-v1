"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
Object.defineProperty(exports, "useScrollPaddingRefs", {
    enumerable: true,
    get: function() {
        return useScrollPaddingRefs;
    }
});
const _react = _interop_require_wildcard(require("react"));
const _resizeobserverpolyfill = _interop_require_default(require("resize-observer-polyfill"));
const _use_isomorphic_layout_effect = require('../../hydration/use_isomorphic_layout_effect');
const _ref_effect = require("./ref_effect");
const _scroll_paddingcss = _interop_require_wildcard(require("./scroll_padding.css"));
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {
        __proto__: null
    };
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
function useScrollPaddingRefs() {
    const containerRef = _react.useRef(null);
    (0, _use_isomorphic_layout_effect.useIsomorphicLayoutEffect)(()=>{
        const container = containerRef.current;
        if (container == null) return;
        container.classList.add(_scroll_paddingcss.default.container);
        return ()=>container.classList.remove(_scroll_paddingcss.default.container);
    }, []);
    const setHeaderHeight = _react.useCallback((height)=>{
        const container = containerRef.current;
        if (container == null) return;
        container.style.setProperty(_scroll_paddingcss.customProperties.scrollPaddingTop, `${height}px`);
    }, []);
    const setFooterHeight = _react.useCallback((height)=>{
        const container = containerRef.current;
        if (container == null) return;
        container.style.setProperty(_scroll_paddingcss.customProperties.scrollPaddingBottom, `${height}px`);
    }, []);
    const headerRef = useElementScrollPadding(setHeaderHeight, _scroll_paddingcss.default.header);
    const footerRef = useElementScrollPadding(setFooterHeight, _scroll_paddingcss.default.footer);
    return {
        containerRef,
        headerRef,
        footerRef
    };
}
function useElementScrollPadding(callback, className) {
    const height = _react.useRef(0);
    return (0, _ref_effect.useRefEffect)((element)=>{
        element.classList.add(className);
        const observer = new _resizeobserverpolyfill.default((entries)=>{
            const entry = entries[0];
            const currentHeight = entry.contentRect.height;
            if (currentHeight != null) {
                height.current = currentHeight;
                if (!element.contains(document.activeElement)) callback(currentHeight);
            }
        });
        observer.observe(element);
        function handleFocusChange() {
            callback(element.contains(document.activeElement) ? 0 : height.current);
        }
        element.addEventListener('focusin', handleFocusChange);
        element.addEventListener('focusout', handleFocusChange);
        return ()=>{
            element.classList.remove(className);
            observer.disconnect();
            element.removeEventListener('focusin', handleFocusChange);
            element.removeEventListener('focusout', handleFocusChange);
        };
    });
}
