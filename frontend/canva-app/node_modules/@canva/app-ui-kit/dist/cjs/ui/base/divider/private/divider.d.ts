import * as React from 'react';
export type DividerDirection = 'horizontal' | 'vertical';
export type DividerProps = {
    direction?: DividerDirection
};
export declare const Divider: ({ direction }: DividerProps) => React.JSX.Element;
